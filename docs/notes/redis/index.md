# Redis

Redis（Remote Dictionary Server）是一个基于内存的高性能键值数据库，同时支持丰富的数据结构和多种应用场景。它最初用于解决数据库访问压力问题，通过将热点数据存储在内存中，减少磁盘数据库访问，提高系统响应速度和吞吐能力。

随着互联网应用的发展，Redis 已经不仅仅是一个缓存工具，而逐渐成为后端系统中的基础组件。它提供了字符串、列表、哈希、集合、有序集合等多种数据结构，并通过事件驱动模型、持久化机制、主从复制、哨兵和集群等设计，实现高性能、高可用的数据服务。